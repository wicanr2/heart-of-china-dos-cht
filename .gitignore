# ===== Copyrighted game data — NEVER commit (derivative localization work only) =====
*.zip
# Game install dirs (any language)
game/
game_en/
game_*/
# Extracted game resources (bitmaps, scripts, fonts, songs — all copyrighted)
extracted/
extracted_*/
res/
res_*/
# Original game binaries / volumes / index
*.EXE
*.exe
VOLUME.*
*.RMF
TANK.*
HOC-SAVE.ZIP
# Extracted source scripts (copyrighted original text) — regenerate locally via extract_dds.py
dialogs_en.json
dialogs_*.json

# ===== Build artifacts =====
*.pyc
__pycache__/
build/
scummvm-src/
scummvm/
*.AppImage
*.dcjk
fonts/*.dcjk
dist/
autopilot.txt
autopilot_shots/

# ===== Screenshots with game art (showcase curated ones committed explicitly) =====
screenshots/_*.png
scripts/_*.sh

# ===== BIOS / disc images =====
*.chd
*.bin
*.cue
*.iso
*.7z
*BIOS*.zip

# Translation intermediates (src_units.json = copyrighted EN script; regenerate via prep_translation.py)
i18n/

# Copyrighted reference materials (軟體世界 manual/magazine scans) — reference only, NEVER publish
攻略/
*.pdf
*.rar

# Title-design working frames (raw game art — local only)
design/

# Copyrighted news/column archive (聯合報 etc.) — reference only, NEVER publish
新聞/
